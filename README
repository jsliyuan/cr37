This is a computer-assisted proof that CR(3, 7) = 20, where CR(r, n) denotes the covering radius of Reed-Muller codes RM(r, n).

Example.cpp is used to verify that the nl3 of fn10||fn6(A^{-1}x+b)+g cannot achieve 21.

-g++ -o example.out example_orbit.cpp boolean_fun.cpp affine_trans.cpp
-example.out
